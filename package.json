{
    "private": true,
    "scripts": {
        "dev": "npm run development",
        "development": "mix",
        "watch": "mix watch",
        "watch-poll": "mix watch -- --watch-options-poll=1000",
        "hot": "mix watch --hot",
        "prod": "npm run production",
        "production": "mix --production",
        "build-amd": "npm run bundle-amd && npm run minify-amd",
        "start": "node web.js",
        "postinstall": "bower cache clean && bower install && npx mix && php artisan vendor:publish --tag=lfm_config && php artisan vendor:publish --tag=lfm_config && php artisan vendor:publish --tag=lfm_public && php artisan vendor:publish --tag=lfm_view --force && php artisan storage:link"
    },
    "devDependencies": {
        "axios": "^0.21",
        "laravel-mix": "^6.0.6",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14",
        "sweetalert2": "^11.0.20"
    },
    "engines": {
        "node": "15.14.0",
        "npm": "7.7.6",
        "bower": "1.8.12"
    },
    "dependencies": {
        "bower": "^1.8.12"
    }
}
